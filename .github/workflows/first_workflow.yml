name: first workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: first step
        run: echo "Welcome to my first workflow"
      - name: second step
        run: echo "Good bye!"

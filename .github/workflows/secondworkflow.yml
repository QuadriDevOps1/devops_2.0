name: second workflow
on: workflow_dispatch
jobs:
  first-job: 
    runs-on: Ubuntu-latest
    steps:
      - name: Print some message
        run: |
          echo "Welcome to second workflow"
          echo "Good luck!"
